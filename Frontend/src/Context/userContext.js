/**
 * User Context Provider
 * 
 * This context manages user data and authentication state across the application.
 * It provides:
 * - User profile information (name, email, username,etc)
 * - Authentication state management
 * - Persistent user data storage in cookieparser
 * 
 * The context is used throughout the app to:
 * - Display user information in components
 * - Check authentication status for protected routes
 * - Handle login/logout functionality
 */
